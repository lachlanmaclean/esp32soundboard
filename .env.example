# --- Shared / Docker Compose ---
POSTGRES_PASSWORD=changeme
PORTAL_URL=http://localhost:3000
# Server's public domain (the one the CYD and the browser both hit directly).
API_PUBLIC_URL=http://localhost:4000

# --- Discord application (Developer Portal) ---
# One app covers both the bot and OAuth2 login.
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
DISCORD_BOT_TOKEN=

# --- apps/server ---
DATABASE_URL=postgresql://gooseboard:changeme@localhost:5432/gooseboard
PORT=4000

# --- apps/portal ---
NEXTAUTH_URL=http://localhost:3000
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=
# Only needed outside Docker Compose, where service DNS names don't resolve.
SERVER_INTERNAL_URL=http://localhost:4000
NEXT_PUBLIC_API_URL=http://localhost:4000
